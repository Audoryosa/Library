/**
 * The main service handling the use of library.
 * An example of how to use the library:<br>
 * <code>
 *     ILibrary library = LibraryFactory.getInstance();
 *     library.addPublication(PublicationFactory.getInstanceOf(publicationType, title, author, pages, isbn, releaseYear));
 * </code>
 */
package com.audriuskumpis.library.service;